#This will be the main
import os
from openpyxl import *
import datetime

#first, we initialize and make sure that the file exists
    #We create if not existing, and we return if it does
    #The file name is Delivery_Stats_{Year Name}
#We then 

#We then start a gui asking whether we 
